  name: Generate Snake Animation

# ==================== 触发条件 ====================
on:
  # 定时任务：每天 UTC 时间 0 点运行
  schedule:
    - cron: "0 0 * * *"

  # 允许手动触发
  workflow_dispatch:

  # 推送到 main 分支时触发
  push:
    branches:
      - main

# ==================== 任务定义 ====================
jobs:
  generate:
    permissions:
      contents: write

    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      # 步骤 1: 检出代码
      - name: 检出仓库代码
        uses: actions/checkout@v3

      # 步骤 2: 生成贪吃蛇动图
      - name: 生成贪吃蛇贡献图
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}

          # ==================== 颜色配置 ====================
          # 配色方案：天蓝蛇 + 紫色渐变 🐍💜
          #
          # 颜色说明：
          # - 背景色：     #1A1D29 (深灰蓝，能看到格子)
          # - 蛇的颜色：   #87CEEB (天蓝色)
          # - 贡献渐变：   淡紫 → 浅紫 → 中紫 → 深紫 → 浓紫
          #
          # 贡献点颜色详解（从低到高）：
          # #2A2D3A - 深灰蓝背景（无贡献，淡淡可见）
          # #B8A4D4 - 淡紫色（少量贡献）
          # #9D7FBF - 浅紫色（中等贡献）
          # #7B5FA7 - 中紫色（较多贡献）
          # #5A3D8F - 深紫色（大量贡献）
          # ================================================

          outputs: |
            dist/snake.svg?color_snake=#0A1A3A&color_dots=#EDEDED,#B8A4D4,#9D7FBF,#7B5FA7,#5A3D8F
            dist/snake-dark.svg?palette=github-dark&color_snake=#0A1A3A&color_dots=#EDEDED,#B8A4D4,#9D7FBF,#7B5FA7,#5A3D8F
            dist/snake-light.svg?palette=github-light&color_snake=#0A1A3A&color_dots=#EDEDED,#D4C5E8,#B8A4D4,#9D7FBF,#7B5FA7



        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 步骤 3: 推送到 output 分支
      - name: 推送到 output 分支
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

# ============================================
#   配色方案说明
# ============================================
#
# 🎨 当前配色：天蓝蛇 + 紫色渐变
#
# 蛇的颜色：
# #87CEEB - 天蓝色（Sky Blue）
#
# 贡献点渐变（紫色系）：
# 1️⃣ #2A2D3A - 深灰蓝（背景，淡淡可见格子）
# 2️⃣ #B8A4D4 - 淡紫色
# 3️⃣ #9D7FBF - 浅紫色
# 4️⃣ #7B5FA7 - 中紫色
# 5️⃣ #5A3D8F - 深紫色
#
# ============================================
#   其他推荐配色方案
# ============================================
#
# 🌸 方案 2：天蓝蛇 + 粉紫渐变（更柔和）
# color_snake=#87CEEB
# color_dots=#2A2D3A,#E0BBE4,#D291BC,#C77DFF,#9D4EDD
#
# 🌊 方案 3：天蓝蛇 + 蓝紫渐变（冷色调）
# color_snake=#87CEEB
# color_dots=#2A2D3A,#B8B5FF,#9B8FFF,#7B68EE,#5B4FD8
#
# 🦄 方案 4：天蓝蛇 + 梦幻紫渐变（高对比）
# color_snake=#87CEEB
# color_dots=#1E1E2E,#CDB4DB,#BF9FD4,#A78BCA,#7B2CBF
#
# 💎 方案 5：天蓝蛇 + 宝石紫渐变（华丽）
# color_snake=#87CEEB
# color_dots=#2A2D3A,#DDA0DD,#BA55D3,#9932CC,#6A0DAD
#
# ============================================
#   快速修改指南
# ============================================
#
# 修改蛇的颜色：
# 找到 color_snake=#87CEEB，替换为你想要的颜色
#
# 修改贡献点渐变：
# 找到 color_dots=#2A2D3A,#B8A4D4,#9D7FBF,#7B5FA7,#5A3D8F
# 按照 背景,淡色,浅色,中色,深色 的顺序修改
#
# ============================================



